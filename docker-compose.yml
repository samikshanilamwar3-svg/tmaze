services:
  netflix-tvmaze:
    build:
      context: .
      args:
        VITE_TVMAZE_API_BASE_URL: https://api.tvmaze.com
    container_name: netflix-tvmaze
    ports:
      - "8081:80"
    restart: unless-stopped
